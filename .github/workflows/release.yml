name: CI Workflow

on:
  push:
    branches: [main]

jobs:
  quality_checks:
    uses: NHSDigital/eps-workflow-quality-checks/.github/workflows/quality-checks.yml@v4.1.1
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  tag_release:
    needs: quality_checks
    uses: NHSDigital/eps-workflow-semantic-release/.github/workflows/tag-release.yml@13024fccca97e125d448ca9bc4616cfd413197fe
