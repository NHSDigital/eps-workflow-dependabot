name: CI Workflow

on:
  push:
    branches: [main]

jobs:
  quality_checks:
    uses: NHSDigital/eps-workflow-quality-checks/.github/workflows/quality-checks.yml@v4.1.1
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  tag_release:
    needs: quality_checks
    uses: NHSDigital/eps-workflow-semantic-release/.github/workflows/tag-release.yml@6c5d2bf097710f0f7328e671ce378ec5681f39cf
