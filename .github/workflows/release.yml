name: CI Workflow

on:
  push:
    branches: [main]

jobs:
  quality_checks:
    uses: NHSDigital/eps-workflow-quality-checks/.github/workflows/quality-checks.yml@v4.1.1
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  tag_release:
    needs: quality_checks
    uses: NHSDigital/eps-workflow-semantic-release/.github/workflows/tag-release.yml@275a3e7bc3efa0db1097c10225f8ee1cfbf5282f
